{
  "name": "aegis",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "frontend": "npm --prefix frontend run dev -- --host localhost",
    "desktop": "concurrently -k \"npm run frontend\" \"npm --prefix desktop start\"",
    "backend": "cd backend && .venv/bin/python -m uvicorn app.main:app --host 127.0.0.1 --port 8000"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}